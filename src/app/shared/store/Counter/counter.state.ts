import { counterModel } from "../../counter.interface";

export const initialState: counterModel = {
    counter: 0,
    channelname: 'Coding Fun'
}

// import { CounterState } from "../counter.interface";

// export const initialState: CounterState = {
//     count: 0,
//   };

